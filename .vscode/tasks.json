{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "compile",
            "command": "g++ -std=c++17 -g -o ${workspaceFolder}/a.out ${file}",
            "type": "shell",
            "args": [],
            "problemMatcher": [
                "$tsc"
            ],
            "presentation": {
                "reveal": "always"
            },
            "group": "build"
        }
    ]
}